apiVersion: v1
kind: Service
metadata:
  name: email-service
  namespace: notification-system
spec:
  selector:
    app: email-service
  ports:
    - port: 8082
      targetPort: 8082
  type: ClusterIP
